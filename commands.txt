Clone and switch to the changed-design branch:
git clone https://github.com/cheedli/FARDI.git
cd FARDI
git checkout changed-design

Backend:
cd backend && uvicorn main:app --host 0.0.0.0 --port 5010 --reload

Frontend:
cd frontend && npm run dev
